- [x] Inspect repo structure and relevant files (app.py, src/*, data/*).
- [x] Add `data/teacher.csv` with columns: TeacherID, TeacherName, Age, Gender, Expertise, YearsOfExperience, TeacherRating.
- [x] Update `src/config.py` to include TEACHERS schema.
- [x] Update `src/data_loader.py` to optionally load/validate teacher CSV and return `df_teacher`.
- [x] Update `app.py` to add Teacher CSV uploader and pass `uploaded_teachers` into loader.
- [x] Add helper `src/teacher_utils.py` (future use / light validation).
- [x] Update `data/users.csv`, `data/courses.csv`, `data/transactions.csv` to match expanded schema headers (UserName/Email, CourseName/CoursePrice/CourseDuration, TransactionID/PaymentMethod/TeacherID).

